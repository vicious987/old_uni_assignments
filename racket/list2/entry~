#lang racket


(define (dist x y)
  (abs (- x y)))

(define (average x y)
  (/ (+ x y) 2))

(define (square x)
  (* x x))

(define (inc i)
  (+ i 1))

(define (cube n)
  (* n n n))

(define (comp x y)
  (lambda (arg) (x (y arg))))